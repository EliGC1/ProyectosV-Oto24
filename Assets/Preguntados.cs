using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Preguntados : MonoBehaviour
{
    float pantallaAltura = Screen.width;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
       
    }
}

/*Instrucciones - 23/09/24: 
     * 1. Van utilizar el componente Text para colocar una pregunta en el medio de la pantalla
     * 2. Van a colocar dos respuestas, también con el componente Text, una de cada lado. 
     * 3. Van a detectar de qué lado presionó (OJO! no son botones), para ver si eligió la respuesta correcta
     *    o la incorrecta. 
     * Extras: 
     * 4. Van a colocar por código tres preguntas diferentes. 
     * 5. Dependiendo de cuántas respondió bien, le darán un resultado. 
     * 
     * Como referencia pueden ver preguntados o Tinder. 
     * 
     * */
